---

- project:
    name: Juniper/contrail-third-party-packages
    check:
      jobs:
        - noop
    experimental:
      jobs:
        - publish-third-party-packages:
            files:
              - upstream/rpm/.*
    gate:
      jobs:
        - noop
